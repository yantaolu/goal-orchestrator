---
name: goal-orchestrator
description: "把用户一句‘我要、我想、我的目标是’之类的简单目标，主动理解为可规划意图，形成有参考价值的具体方案、目标规划、情景推演与现实实施指南。用户明确索要产品、技术、业务或组织方案，提出管理、经营、产品、组织变革或个人目标，要求推演某个运行场景，或要求落实既有方案时都应使用；不要求用户先懂专业术语，也不因信息不完整反复提问。先形成可条件推进的版本，再按问题复杂度补足机制、阶段、判断门与调整路线。模拟团队只在复杂协作确有价值时使用；不替代真实执行，也不用于普通事实问答、单步操作或明确的数据导入等执行任务。"
---

# 目标实现推演

用户只需要说清想实现什么。本 Skill 的核心价值，是把一句自然表达主动构造成可规划意图，再形成能够指导判断和行动的方案；交付物路由、实施拆分和虚拟组织都是服务这个过程的工具，不是起点。

方案优先仍然成立：明确索要方案时，把专业深度留给方案本身；提出目标时，给出方案、规划和实施路径；已有方案时尊重基线。自然表达不等于简略，规划也不能退化为几句概念。

## 先构造可规划意图

先理解用户想让现实发生什么变化，再判断交付形式。保留用户原话，并在内部形成暂定 Goal Brief：

- 期望的现实变化，以及直接对象或受益者。
- 能表明目标正在实现的成功信号或完成状态。
- 已知范围、约束、期限、资源和明确排除项。
- 会影响路线的关键未知。
- 为继续推进所需的最小可逆假设，以及必要时的条件分支。

不要把内部 Goal Brief 写成问卷。能用合理、可逆的假设形成条件式方案时，先给出完整版本，把一两个高杠杆问题放在结尾用于校准。只有某个未知会导致完全不同的路线、重大不可逆成本或安全、法律、资金风险时，才在回答前问一个简短问题。

短输入不是低复杂度。“我要管理 2000 人的事业部”虽然只有一句话，但隐含组织层级、授权、经营节奏、干部梯队和持续运营，应该直接转译为管理操作系统目标并深入规划，而不是先问“你要方案还是实施方案”。

## 再判断交付意图

这些标签用于内部路由，不要默认展示：

- `solution_design`：用户明确要求产品方案、技术方案、业务方案、组织架构、选型或比较。方案是主体；实施只保留影响方案成立的内容，默认不创建虚拟组织。
- `goal_realization`：用户表达想实现的结果，但尚未形成完整路径。先给出有实质内容的解决方案，再给出目标规划、情景推演和与复杂度匹配的实施路径。
- `implementation_orchestration`：用户已有或指定方案、技术栈、架构或决策，要求排期、迁移、分工、交付或运营。尊重既定方案，除非发现阻断冲突或重大风险；重点说明怎样实施。

混合请求先明确主次。例如“先比较架构，再给落地计划”先完成比较和推荐，再展开实施；“给我组织架构方案，不要实施计划”则深入设计真实组织，不追加元层虚拟团队或完整排期。

## 独立选择五种深度

不要用用户输入的字数或某一个维度替代其他维度。

### 方案状态与方案深度

`solution_status`：

- `to_design`：需要从零形成方案。
- `partially_defined`：已有方向或约束，仍需补足关键设计。
- `fixed`：核心方案已确定，默认作为实施基线。

`solution_depth`：

- `concise`：仅当用户明确要求概要、快速判断、一页结论或同等压缩交付时使用。压缩展开而不丢决策闭环：保留推荐与关键机制或理由、至少一个适用边界或升级条件、一个关键验证门。可以自然合在一两段里。
- `standard`：普通“给我一份方案”的默认值。给出足以讨论、选择和评审的领域方案，不能只有方向或名词。
- `deep`：用户明确要求详细、完整、可落地、评审级，或问题本身复杂、高风险、跨系统且关键约束很多时使用。

`standard` 按相关性说明推荐与边界、核心模块和职责、数据／控制／业务流、关键取舍、不适用条件、异常或主要风险、验证方法和判断标准。`deep` 再展开真正相关的接口、数据、状态、安全、性能、兼容、可观测性、运维或验收机制。深度来自领域机制，不来自堆标题。

### 规划深度

`planning_depth` 决定目标需要多强的参考指南：

- `light`：单一、低风险、短周期、易调整的目标。给出清楚路径、节奏、检查点和第一步，不把生活目标项目化。
- `standard`：`goal_realization` 的默认下限。形成可规划目标、目标状态或运营机制、关键结果、依赖、阶段成果、判断门、失败路线、调整信号和现实下一步。
- `deep`：多利益相关者、多组织层级、强系统耦合、高风险、长周期、难逆转或需要持续运营的目标。形成完整目标模型和运行机制，并通过代表性场景发现和修订缺口。

按规模、涉及人员与利益相关者、组织层级、系统耦合、风险、时间跨度、可逆性和持续运营复杂度判断，而不是按句子长短。管理 2000 人、多业务单元、组织变革等通常使用 `deep`；三个月学会游泳可用 `light` 或 `standard`。

### 情景推演深度

`simulation_depth` 与组织深度独立：

- `none`：纯说明、低复杂度目标或现实场景不会改变方案。
- `light`：选择一个最能暴露缺口的正常、压力或失败场景，沿路径向前推演，并说明发现怎样改变计划。
- `structured`：选择少量代表性正常、压力和失败场景，跟踪触发、决策、交接、结果和恢复；把发现落实为权限、机制、阶段、门槛或备用路径的具体修订。

推演是对目标、路径或运行机制的前向模拟，不等于列风险清单。若推演没有改变任何内容，就说明场景选择或分析还不够有辨识力。管理 2000 人、组织变革、关键运营系统等通常需要 `structured`；用户只要求推演某个重大客诉决策链时，可以做聚焦的场景推演，而不创建完整虚拟团队。

### 实施与组织深度

`implementation_depth`：

- `none`：用户只要方案，实施内容没有额外价值。
- `light`：给出影响方案成立的验证顺序、依赖、原型或现实首步。
- `detailed`：用户要目标实现或既有方案落地，展开阶段、责任、验收、发布与回退。

`organization_depth`：

- `none`：直接给方案、规划或步骤。
- `responsibility_map`：多类责任或交接会影响实施，只说明真实责任、交付和验收，不创建 `R0`。
- `simulated_team`：复杂跨专业生产、交接、独立验证、高风险审查，或用户明确要求虚拟团队时，才构建用于生成和审查方案的完整模拟组织。

请求类型不直接决定是否需要组织。复杂规划可以没有虚拟团队；明确技术方案不因专业性强就自动组队。

## 按模式形成内容

### 明确索要方案

以 `solution_depth` 相称的方案直接回答。用户没有要求实施计划时，不扩写排期和团队；但实施从轻绝不能使方案变浅。组织架构方案中的部门、层级和岗位是方案所设计的真实组织，不是生成方案的虚拟团队。

比较类方案只有在推荐取决于运行表现、兼容性或体验时才使用受控双原型：两种候选使用同一代表性真实数据集、目标设备或系统、任务和预算；两者都在最终选型前完成最小可比垂直切片，两者结果齐备后再应用预先定义的保持或切换阈值。候选轴不能被平台轴替代。用户未给数值时使用待填预算变量，不虚构数字。当前候选全部硬门通过则保持；只有它至少一项失败、另一候选在相同条件下通过且没有新增关键阻碍时才切换。没有真实完成双候选对照时，只给初步或倾向性推荐；纯成本、政策或合规比较使用相应证据门。

### 只给出目标

阅读并执行 [references/goal-planning-protocol.md](references/goal-planning-protocol.md)。先给出你对目标的暂定转译和推荐方向，再按 `planning_depth` 形成目标状态、机制、结果拆解、路径、阶段、判断门和现实第一步。按 `simulation_depth` 推演代表性场景，让发现实际修改方案。

目标规划的信息价值门是：读者应能据此决定做什么、先做什么、何时继续、何时调整以及失败时走哪条路。若答案只有愿景、原则、宽泛阶段或管理口号，应继续补足领域机制，而不是通过增加标题、角色或篇幅伪装完整。

### 实施既有方案

复述不可改变的决定和实施目标，将 `solution_status` 视为 `fixed`。围绕工作拆分、顺序、依赖、责任、质量门、发布、运行、反馈和回退形成计划，不重新发明选型。只有内部矛盾、遗漏会阻断实施或产生重大安全、合规、成本、兼容风险时，才提出局部修订并说明影响。

## 分清三种组织概念

不要把以下三者混在一起：

1. **目标中的真实组织：** 例如事业部、业务单元、区域团队、管理层级和岗位，是方案要设计或改善的对象。
2. **组织运行情景推演：** 例如战略变化、连续不达标、重大客诉、核心干部离职，用来检验真实组织怎样决策、交接和恢复。
3. **生成方案的虚拟团队：** 生产者、验证者和整合者协作形成并审查答案，只是可选的方法扩展。

前两者可以在 `organization_depth: none` 下深入展开。只有第三者属于 `simulated_team`。

选择 `simulated_team` 时才读取 [references/role-schema.md](references/role-schema.md)；需要结构化角色时再读取 [references/role.schema.json](references/role.schema.json)，并执行 [references/simulation-protocol.md](references/simulation-protocol.md)。创建且仅创建一个协调者 `R0`，首次自然写成“目标实现协调者（R0）”或“协调负责人（R0，唯一）”。关键成果的独立验证者必须不同于其设计者和实现者，`R0` 也不担任独立验证者。只展示真正改变方案的审查和返工。

未选择 `simulated_team` 时，不创建 `R0`，不补完整角色表，也不意味着不做目标或场景推演。

## 保持证据与现实边界

区分用户事实、可逆假设、推导结论、推演发现和现实未知。不要声称已经真实开发、测试、部署、联系用户、产生收益、获得批准或观察到指标，除非当前工作流确实取得这些证据。方案内部一致不等于现实成功。

普通回答遵循上述原则即可；`deep` 方案、正式可行性判断、复杂报告或结构化交接时再读取 [references/output-contracts.md](references/output-contracts.md)。边界提示不是固定开场，只在使用虚拟团队、用户要求现实执行但本轮没有执行，或读者可能把方案审查误解为现实结果时自然说明一次。

## 自然组织答案

先直接回答，再解释依据。标题跟随领域内容，不展示内部路由、深度标签、证据枚举或检查表。复杂不等于僵硬：不固定 Mermaid、九阶段生命周期、全套团队或报告大纲；简单目标也不无故膨胀。

需要独立报告时，从 [assets/orchestration-report-template.md](assets/orchestration-report-template.md) 选取相关模块并删除无关部分。遇到意图边界或需要校准示例时读取 [references/examples.md](references/examples.md)。

## 结构化交付

只在用户要求、系统交接或审计需要时输出结构化数据。根对象使用 `schema_version: "3.3"`，记录：

- `request_mode`、`solution_status`、`solution_depth`
- `planning_depth`、`simulation_depth`
- `implementation_depth`、`organization_depth`
- Goal Brief、方案、规划、推演修订、证据和未执行的外部动作

完整角色对象仍使用 `schema_version: "3.0"`；根契约升级不改变角色契约。

## 回答前检查

按请求选择必要检查，不让检查表控制文风：

1. **意图构造：** 是否把原话转成了可规划的现实变化、对象、成功信号和必要假设，而不是把路由问题问回用户？
2. **交付匹配：** 方案、目标实现和既有方案实施是否各自抓住主体？
3. **规划价值：** 目标规划是否包含相关的目标状态、机制、结果、依赖、阶段成果、判断门、失败路线、调整信号和现实首步，而非寥寥概念？
4. **复杂度匹配：** 是否避免把短输入当成简单目标，也避免把简单生活目标项目化？
5. **推演闭环：** 代表性场景是否沿路径向前运行，并产生至少一项可见的方案修订？
6. **组织分离：** 真实组织、组织运行推演和虚拟团队是否清楚分开？组织是否采用最轻且足够的形式？
7. **方案完整：** `concise` 是否仍保留边界与验证门；`standard` 和 `deep` 是否达到领域机制深度？
8. **实施连续：** 需要实施时，阶段、依赖、责任、产出、验收和回退是否闭环？
9. **证据诚实：** 是否没有把假设、推演或方案级审查写成现实结果？
10. **比较纪律：** 若最终选型依赖双原型，是否在相同条件下完成两个候选并在结果齐备后才决策？否则是否明确只是初步推荐？

发现不足时补充有决策价值的领域内容或重组答案；不要用流程套话、角色数量和固定格式填空。

## 维护与验证

修改后运行：

```text
python3 scripts/check_consistency.py .
```

再运行 Skill Creator 的 `quick_validate.py`，并用 [evals/evals.json](evals/evals.json) 与 [evals/trigger-evals.json](evals/trigger-evals.json) 做行为和自然触发回归。
